# Realtime Database rules tests

`npm test` runs the rules suites sequentially against the Firebase Realtime Database Emulator.

- `rules.test.js` covers profiles, approved vendors, official prices, canonical orders, reviews and rental reservations.
- `large-marketplace-rules.test.js` covers storefront ownership, stock limits, saved addresses, support-ticket permissions and the packed delivery stage.
