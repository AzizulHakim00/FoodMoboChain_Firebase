{
  "name": "foodmobochain-spark-rules",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test test/rules.test.js",
    "test:rules": "node --test test/rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^14.8.0"
  }
}
